import React from 'react';
import {observer} from 'mobx-react-lite';
import {Box} from '../component/Box';
import {Text} from '../component/Text';

export const StartEndDateFilterScreen: React.FC = observer(() => {
  return (
    <Box>
      <Text>dasdasfsda</Text>
    </Box>
  );
});
